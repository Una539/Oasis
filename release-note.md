## Oasis Flow 0.8.0

### 全局搜索

- 想不起那件事放在哪？现在直接搜。桌面端和移动端都有了顺手的搜索入口。
- 不管它躺在今日、以后、灵感，还是已经完成，都能被找回来。
- 除了任务内容，也能搜日期和优先级：试试 `p1`、`!3`。
- 找到以后不用再跳回原来的列表：完成、修改、删除、调日期、调优先级、开提醒，都能当场搞定。

### 完成统计

- 这一周做了多少事、连续坚持了几天，终于有地方可以看了。
- 近 7 天的完成趋势会留下一条小小的轨迹；统计页底部还能翻到最近完成的 5 件事。

### 体验调整

- 移动端首次启动会询问通知权限。给今日或逾期待办开了提醒，就让 Oasis 每天提醒你一次。
- 手机上的待办更像在和你配合：点一下完成，长按改内容；日期标签点一下开关提醒，长按改日期。
- 底部输入区、搜索面板和键盘相处得更融洽，输入时少一点遮挡，多一点从容。
- 优先级换上了更轻快的马卡龙配色，按钮也更好按了。

## 安装说明

- Windows: 下载 `oasis_VERSION_win.msi` 并运行安装
- macOS: 下载 `oasis_VERSION_mac_ARCH.dmg` 并拖拽到 Applications
- Linux: 下载 `oasis_VERSION_linux_ARCH.AppImage`、`oasis_VERSION_linux_ARCH.deb` 或 `oasis_VERSION_linux_ARCH.rpm`
- Android: 下载 `oasis_VERSION_android_ARCH.apk` 并安装到 Android 设备

文件名中的 `VERSION` 会替换为实际版本号，例如 `oasis_0.8.0_win.msi`。

## 许可证

本项目采用 GPL-3.0-or-later 许可证。
